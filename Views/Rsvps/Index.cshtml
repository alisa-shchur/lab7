@model IEnumerable<Rsvp>

@{
    ViewData["Title"] = "Список ответов";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Список ответов</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                <span>Имя</span>
            </th>
            <th>
                <span>E-mail</span>
            </th>
            <th>
                <span>Номер телефона</span>
            </th>
            <th>
                <span>Согласие</span>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Phone)
                </td>
                <td>
                    @{
                        if (item.WillAttend) {<span>Да</span>}
                        else {<span>Нет</span>}
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
